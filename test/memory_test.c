#include "memory_test.h"

START

TEST(Memory, Run) {
  /* Just make sure that the function works on your operating system. The
   * actual value is hardware-dependent.
   */
  memory_get_total();
}

FINISH
